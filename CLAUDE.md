# CLAUDE.md

1日のスケジュールを表示する静的サイト。GitHub Pages で公開している。

## 構成

- `index.html` — 表示ロジック。**予定の変更でここを触る必要はない。**
- `schedule.json` — スケジュールのデータ。**変更依頼は基本すべてこのファイルの編集で対応する。**
- `icon.png` — ホーム画面用アイコン。

## schedule.json の書き方

`days.weekday.blocks` と `days.holiday.blocks` が本体。

- 各ブロックは `start`（開始時刻 `"HH:MM"`）だけを持つ。**終了時刻は書かない。** 次のブロックの `start` が自動的に終了時刻になる。
- 最後のブロックは `24:00` まで続く。
- 必ず時刻の昇順に並べる。最初のブロックは `"00:00"` から始める。
- `line` は `lines` のキーのいずれか（`work` / `gym` / `meal` / `chores` / `study` / `play` / `rest`）。色分けとアイコンの見た目に対応する。
- `note` は任意。1行の補足。省略可。
- `line` が `study` のブロックは、`note` を空にしておくと `studyThemes` の曜日別テーマが自動で入る。固定の注記を出したいときだけ `note` を書く。

### 例

「筋トレを17時からにして」と言われたら、`weekday` の `筋トレ` の `start` を `"17:00"` に変え、前後のブロック（`ひと息` / `クールダウン`）の `start` も辻褄が合うように調整する。ブロックの追加・削除もこのファイル内で完結する。

## 作業ルール

- 変更後は `python3 -m json.tool schedule.json > /dev/null` で JSON が壊れていないことを確認する。
- ブロックが時刻順に並んでいること、`line` が `lines` に存在するキーであることを確認する。
- コミットメッセージは変更内容を日本語で簡潔に（例: `筋トレを17:00開始に変更`）。
- `main` に push すると GitHub Pages が自動で反映する。

## 触らないもの

- `index.html` の見た目やロジックは、明示的に頼まれたときだけ変更する。
- その日限りの調整（残業でずれた、今日はジム休み）はページ上の操作で完結するので、コミットは不要。
